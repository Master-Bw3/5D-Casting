package net.masterbw3.fivedimcasting.client.gui;

import at.petrak.hexcasting.api.casting.eval.ResolvedPattern;
import at.petrak.hexcasting.client.gui.GuiSpellcasting;
import net.minecraft.nbt.NbtCompound;
import net.minecraft.util.Hand;
import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;

import java.util.List;

class GuiGrandStaffSpellCasting(handOpenedWith: Hand,
                                patterns: MutableList<ResolvedPattern>,
                                cachedStack: kotlin.collections.List<NbtCompound>, cachedRavenmind: NbtCompound?,
                                parenCount: Int
)// : GuiSpellcasting(handOpenedWith, patterns, cachedStack, cachedRavenmind, parenCount)
{

}
